Semplice contratto solidity che permette la memorizzazione di un dato sulla blockchain.
La funzione "store" (è una funzione di scrittura) salva una stringa passata come input nella variabile di stato "prov_key".
La funzione "retrieve" (è una funzione di lettura, view) restituisce il valore della variabile "prov_key".
